namespace DevBlog.Web.Utility
{
    public class StaticDetails
    {
        /// <summary>
        /// Enumeration with request types, to avoid random strings.
        /// </summary>
        public enum RequestType
        {
            GET,
            POST,
            PUT,
            DELETE,
        }
    }
}